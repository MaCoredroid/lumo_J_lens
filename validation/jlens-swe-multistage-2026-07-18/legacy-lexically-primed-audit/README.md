# Invalidated lexically primed audit

These deterministic gzip files preserve the first multistage replay byte for
byte. That replay incorrectly labeled S0-S2 as oracle-hidden. The scored target
`lazy` was already semantically exposed inside `SimpleLazyObject` in the task
statement and every captured prefix. The positive target-versus-foil margin is
therefore a lexically primed control, not hidden-concept evidence.

`manifest.json` binds both each compressed file and its decompressed original,
including all eight artifacts declared by the original fresh-run manifest.
The current parent-directory artifacts contain the corrected semantic leakage
classification and supersede these files for analysis.
